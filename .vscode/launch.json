{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Launch Edge localhost",
            "url": "http://localhost:5501",
            "webRoot": "${workspaceFolder}"
        }
    ]
}